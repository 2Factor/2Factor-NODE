M2factor
=================
This file was automatically generated for 2Factor by APIMATIC BETA v2.0 on 02/18/2016


How To Install: 
=============
The generated code relies on node package manager (npm) being available to resolve dependencies.
Once published you will need copy the folder '2factor' in to your 'node_modules' folder.

  
How To Use:
===========
The following shows how import the controllers and use:

1) Import the module:

        var 2factor = require('2factor');
2) Configure any authentication parameters. For example:

        var config = 2factor.configuration;
        config.apikey = a_secret_key;

3) Access various controllers by:

        var controller = 2factor.XYZ;
        controller.getItems(id, callback);
    

